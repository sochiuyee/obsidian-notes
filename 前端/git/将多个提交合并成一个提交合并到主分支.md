```js
git checkout -b [branch_A] // 切换到[branch_A]

git merge --squash [branch_B] // 把[branch_B]合入[branch_A]中，并将多个commit记录合并

git commit -m "commit's log message"  // 填写一个commit记录信息

git push --set-upstream [remote] [branch_A] // 如果是新创建的分支就推送并关联远程分支

git push [remote] [branch_A] // 已经关联的就直接推送
```
